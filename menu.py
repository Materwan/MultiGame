import os
import sys

import pygame

from utils import *


class Principal_Menu(DefaultState):

    def __init__(self, screen: pygame.Surface, manager: BaseManager):

        super().__init__(screen, manager)

    def event(self, events):

        super().event(events)

    def update(self):

        super().update()

    def display(self):

        super().display()
